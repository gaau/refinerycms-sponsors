<ul>
  <% if Admin::SponsorsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_sponsors_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_sponsor_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::SponsorsController.sortable? and Sponsor.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Sponsors"),
                 admin_sponsors_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Sponsors"),
                 admin_sponsors_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>